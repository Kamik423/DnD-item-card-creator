\documentclass{article}
\usepackage[paperheight=74mm,paperwidth=52mm,margin=0mm,bottom=1.5mm]{geometry}

\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage[utf8]{inputenc}
\usepackage{graphicx}

\usepackage{float}

\usepackage{array}
\usepackage{tabularx}
\usepackage[table]{xcolor}
\usepackage{ragged2e}

\begin{document}

    \setlength\parindent{0pt}
    {% for item in items %}
    \colorbox{black}{\parbox{\textwidth}{\color{white} \centering 
        {{ item.name }}
    }}
    \begin{tabularx}{\textwidth}{ >{\centering\arraybackslash}X }
        {{ item.item_type }}{% if item.bonus %} {{ item.bonus }}{% endif %}{% if item.item_type and item.rarity %}, {% endif %}{{ item.rarity }}\\\hline
    \end{tabularx}\\[-0.4mm]
    \begin{tabularx}{\textwidth}{ m{7.5mm} | X }
        {% for option_name in item.options %} 
        \footnotesize {{ option_name }}  & \footnotesize {{ item.options[option_name] }} \\\hline
        {% endfor %}
    \end{tabularx}
    \vfill
    \hspace{.05\textwidth}\parbox{.9\textwidth}{
    \footnotesize
    {{ item.description }}
    }
    \vfill

    \colorbox{black}{\parbox{\textwidth}{\color{white}
    \centering 
    {{ item.time }} {% if not item.time %}\color{black}placeholder{% endif %}
    }}
    \newpage
    {% endfor %}

    
\end{document}